"""Strands tools for on-premises SQL Server queries (Relius/PlanConnect)."""

from __future__ import annotations

# TODO: Implement @tool decorated functions:
# - query_relius(plan_id, ssn) → PersonalInfoByPlan
# - query_employment_status(plan_id, ssn) → jobstatuscurrent
# - query_original_doh(plan_id, ssn) → originalDOH
# - query_contrib_rates(plan_id, ssn) → CurrentContributionRates
# - query_ytd(plan_id, ssn, source) → ERContribYTD
# - query_forfeiture_balance(plan_id) → PayrollForfs
