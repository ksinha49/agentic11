"""Strands tools for DynamoDB business rules access."""

from __future__ import annotations

# TODO: Implement @tool decorated functions:
# - get_client_config(plan_id, pay_freq)
# - get_validation_rules(category)
# - get_calculation_rule(plan_id, calc_type) — with CLIENT→GLOBAL fallback
# - get_irs_limits(year)
# - get_plan_holds(plan_id)
# - get_pipeline_steps(plan_id, pay_freq)
