"""Type aliases used across the BlueStar platform."""

from __future__ import annotations

from typing import Any

JsonDict = dict[str, Any]
BatchId = str
PlanId = str
PayFreq = str
SSN = str
StepOrder = int
