"""Orchestrator Agent definition and Strands wiring."""

from __future__ import annotations

# TODO: Define OrchestratorAgent with Bedrock model (via LiteLLM)
# System prompt: central coordinator, pipeline dispatch, workflow state, escalation
# Tools: load_pipeline, dispatch_step, check_workflow_state, escalate_to_human, emit_event
